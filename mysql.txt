
  
 create database temp default character set utf8 collate utf8_general_ci;
 show databases;
 use temp;

  create table t(id int auto_increment ,title varchar(32),primary key (id));
  show tables;
  desc t; 
  
  alter table t add content varchar(32) default null;
  alter table t drop content;
  alter table t modify title varchar(64);
  alter table t change title tit varchar(32);
  alter table t add index t_index(title);
  alter table t drop index t_index;
  
  insert into t(title)values("ff");
  select count(*) from t where title like "%f"
  select id from t where id <100;
  update t set title ="gg" where id =1;
  
  truncate table t;

  mysqldump -uroot -ppassword temp >temp.sql;
  mysqldump -uroot -ppassword temp t >temp.sql;
  mysql -uroot -ppassword temp< temp.sql
  source temp.sql

  
  show variables like "char%";
  set character_set_client=utf8;
  show engine innodb status;
  show create database temp;
  show create table t\G;
  show table status\G;
  show collation ;